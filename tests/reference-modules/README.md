# Reference Modules

These files are not loaded by `index.html` and are not part of the deployed simulator. They are small reference implementations used by `scripts/validate-runtime-core.mjs` to compare selected runtime behavior against modular, easier-to-test helpers.

Keep this folder focused on validation fixtures. Active browser code belongs in `src/runtime/`, and active styling belongs in `src/styles/`.
